import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

export const theme = {
   ...DefaultTheme,
   colors: {
      ...DefaultTheme.colors,
      primary: '#0582C8',
      secondary: 'white',
      opacityBlack: 'rgba(0, 0, 0, 0.25)',
   },
};
